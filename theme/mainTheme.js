import React from 'react';
import { StyleSheet } from 'react-native';
import { culori } from 'colors';

export const personalizare = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: culori.fundal,
      alignItems: 'center',
      justifyContent: 'center',
    },
  });
  